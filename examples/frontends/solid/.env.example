# Browser requests default to the same origin. Vite proxies these paths in development.
FRAMEKIT_PROXY_TARGET=http://localhost:3000

# Set only when the deployed API is on a different, CORS-enabled origin.
VITE_FRAMEKIT_API_URL=
VITE_FRAMEKIT_TENANT_ID=default
VITE_FRAMEKIT_APP=crm
